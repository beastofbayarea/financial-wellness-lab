# Gemini on Vertex AI. Authentication uses Application Default Credentials.
GCP_PROJECT_ID=cent-capital-472820
GCP_REGION=global
GEMINI_MODEL=gemini-flash-latest
GEMINI_MAX_TOKENS=8192

# Optional explicit service-account file. Keep the JSON file outside this repo.
# GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\service-account-key.json
